si;
2 % si; // signed
2 % ui; // unsigned
ui % 2; // unsigned
ui % si; // unsigned
ui & si; // unsigned
abuf;
&abuf;
abuf.ptr;
abuf.ptr[0];
*abuf.ptr;
(&abuf)->len;
&(&abuf)->len;
put;
put("coucou", 6ul);
put("", 0);
put("na"+1, 1);
put(abuf.ptr, abuf.len);
&put;
si++;
--ui;
si == ui;
ui != !!si;
ui = *&abuf.len;
ui < si ? si+= ui : 0u;
abuf = arry[0];
abuf = *(arry+1);
arry[0] = *(2+arry);
